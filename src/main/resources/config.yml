torch-duration-seconds: 300
max-torches-per-player: 10
refuel:
  coal-seconds: 60
  charcoal-seconds: 30
  allow-others-refuel: false
  max-torch-lifetime-seconds: 3600
messages:
  torch-placed: "&aTorch will expire in {duration} seconds."
  torch-expired: "&7Your torch at {x}, {y}, {z} has expired."
  no-permission: "&cYou don't have permission."
  max-reached: "&cYou have reached the maximum torch limit ({max})."
  reload-success: "&aConfig reloaded."
  torch-refueled: "&6Torch lifetime extended by {added}s. Remaining: {remaining}s."
  torch-refuel-denied: "&cYou can only refuel your own torches."
  torch-refuel-cap: "&cThis torch has reached its maximum lifetime."
